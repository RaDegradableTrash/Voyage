#if UNITY_EDITOR
// Boundary validation is intentionally kept with TerrainTileValidation so the
// baker has one validation entry point. This file remains as a stable asset
// path for projects that already imported the previous editor layout.
#endif

# Voyage 项目协作规则

## Git 分支与提交

- 不得创建新分支，除非用户明确要求创建分支。不得通过 worktree 或其他工具隐式创建新分支。
- 一切临时、非正式的 commit 和 push 均以现有 `snapshot` 分支为目标。
- 一切正式的 commit 和 push 均以现有 `main` 分支为目标。
- 如果所需目标分支不存在，不得自行创建；先告知用户并等待明确指示。
- 切换分支或整理提交时保留用户未提交的修改，不得覆盖或丢弃。
